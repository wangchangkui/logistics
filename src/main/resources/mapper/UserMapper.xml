<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myxiaowang.logistics.dao.UserMapper">

    <select id="getUser" resultType="com.myxiaowang.logistics.pojo.User">
        select user.id,user.idcard,user.password,user.money as decimals,user.header_img as headerImage,user.grande as sex,user.phone,user.create_time,user.update_time,user.username,user.userid
             ,address.address from user left join address on user.userid=address.userid where address.isCheck=1 and user.userid=#{openId}
    </select>
</mapper>